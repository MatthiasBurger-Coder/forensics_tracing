package de.burger.forensics.domain.model;

/**
 * Enumerates the supported rule types.
 */
public enum RuleType {
    IF_TRUE,
    IF_FALSE,
    SWITCH,
    SWITCH_CASE,
    RETURN,
    THROW,
    ENTRY,
    EXIT
}
